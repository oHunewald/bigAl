<header class="navbar navbar-fixed-top navbar-inverse">
  <div class="navbar-inner">
    <div class="container">
      
      <nav>
        <ul class="nav pull-middle">
          <li><%= link_to "Home",    root_path %></li>
          <li><%= link_to "Help",    help_path %></li>
          <li id="fat-menu" class="dropdown">
          	<a href="#" class="dropdown-toggle" data-toggle="dropdown">
          		NGS Projects <b class="caret"></b>
          	</a>
          	<ul class="dropdown-menu">
          		<li><%= link_to "Show all Projects", projects_path %></li>
          		<% if user_signed_in? %>
            		<li><%= link_to "Show my Projects", user_path(current_user) %></li>
                <li><%= link_to "Show my Templates", user_templates_path(current_user) %></li>
            		<li class="divider"></li>
            		<li><%= link_to "Create new Project", new_project_path %></li>
                <li class="divider"></li>
                <li><%= link_to "PGM planning", calendar_path %></li>
                <% if current_user.role == 'Moderator' %>
                  <li class="divider"></li>
                  <li><%= link_to "Approve Projects", not_approved_projects_path %></li>
                  <li><%= link_to "Create Template" , new_template_path %></li>
                <% end %>
          		<% end %>
          	</ul>
          </li>
          <% if user_signed_in? and current_user.libraries.any? %>        
          <li id="fat-menu" class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown">
              Template prep <b class="caret"></b>
            </a>
            <ul class="dropdown-menu">              
                <li><%= link_to "Select Lib for Template", user_libraries_path(current_user) %></li>              
            </ul>           
          </li>
          <% end %>          
          <li id="fat-menu" class="dropdown">
          	<a href="#" class="dropdown-toggle" data-toggle="dropdown">
          		Inventory <b class="caret"></b>
          	</a>
          	<ul class="dropdown-menu">
              <% if user_signed_in? and current_user.role == 'Moderator'%>          
            		<li><%= link_to "Show Inventory", inventories_path %></li>
                <li><%= link_to "New Item", new_inventory_path %></li>
              <% end %>
              <% if user_signed_in? %>  
                <li><%= link_to "My Inventory", my_inventories_path(current_user) %></li>
              <% end %>
          	</ul>           
          </li>
          <% if user_signed_in? and current_user.role == 'Admin'%>
          <li id="fat-menu" class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown">
              Administration <b class="caret"></b>
            </a>
            <ul class="dropdown-menu">
              <li><%= link_to "Show all User", users_path %></li>
              <li><%= link_to "New Inventory Category", new_category_path %></li>
            </ul>
          </li> 
          <% end %> 
        </ul>
        <ul class="nav pull-right">
        <% if user_signed_in? %>
          <li id="fat-menu" class="dropdown">
          	<a href="#" class="dropdown-toggle" data-toggle="dropdown">
          		<%= current_user.name %> <b class="caret"></b>
          	</a>
          	<ul class="dropdown-menu">
              <li><%= link_to "Create News", new_user_sharedmessage_path(current_user) %></li>
				      <li><%= link_to "Edit profile", edit_user_registration_path, :method => :get %></li>
          		<li class="divider"></li>
          		<li><%= link_to "Sign out", destroy_user_session_path, :method => :delete %></li>
          	</ul>
          </li>   		    
		  <% else %>
			<li><%= link_to "sign in", new_user_session_path %></li>
		  <% end %>        	
        </ul>
      </nav>
    </div>
  </div>
</header>