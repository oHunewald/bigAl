<h2>Sample</h2>
<hr/>
<aside>
	<p>Name: <%= @sample.name %></p>
	<p>Size (bp): <%= @sample.size %></p>
	<p>Quantity (ng/ul): <%= @sample.quantity %></p>
	<p><%= link_to "Edit", edit_project_sample_path(@project, @sample) %></p>
	<p><%= link_to "Create Library", 
		new_project_sample_library_path(@project, @sample) %></p>
	<p><%= link_to "Back to Project", @project %></p>
</aside>
<article>
	<%= render 'layouts/librariesTable_table' %>
</article>
