<%= form_for [@user, @project] do |f| %>
  
  <div class="field">
    <%= f.text_field :name, placeholder: "Create new Project..." %>
    <%= f.text_area :description, placeholder: "Please give a description" %>
  </div>
  <%= f.submit %>
<% end %>